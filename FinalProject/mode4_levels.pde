void levels() {
  if (mode==4) {
    /*
    display level selection text
     if mousex and mousey is within rectangle and pressed
     change mode
     do this for each level
     */
    if (mousePressed) {
      mode = 5;
    }
  }
}